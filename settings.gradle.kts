// the short name of your library. This string will name relevant files and folders.
// Such as:
// <rootProject.name>.jar will be the name of your build jar
// <rootProject.name>.zip will be the name of your release file
rootProject.name = "myLibrary"
